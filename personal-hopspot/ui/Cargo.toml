[package]
name = "personal-hopspot-ui"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false
description = "Shared UI functionlity for the Personal Hopspot"

[dependencies]
personal-rns = { path = "../../personal-rns", default-features = false }
embedded-graphics = "0.8" # pinned to the same 0.8 the S3's ssd1306 stack uses.
heapless = { version = "0.8", default-features = false }

[lints]
workspace = true
