// route-id: contacts.merge

import { CatalogPlaceholderRoute } from "@/features/placeholder-screen";

export default function MergeContactsRoute() {
  return <CatalogPlaceholderRoute screenId="contacts.merge" />;
}
