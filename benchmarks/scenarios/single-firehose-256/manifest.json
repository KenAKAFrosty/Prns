{
  "name": "single-firehose-256",
  "version": 2,
  "description": "The single-firehose at a deep window (256 in flight): a ProveAll destination saturated with windowed SINGLE packets for a fixed wall-time. The deep variant surfaces what shallow windows hide - whether inbound decrypt/verify serialize on the reactor or parallelize off it.",
  "roles": [
    "responder",
    "initiator"
  ],
  "profile": {
    "mechanism": "single",
    "window": 256,
    "duration_ms": 30000,
    "payload_min": 60,
    "payload_max": 380
  },
  "conformance": "sent == delivered + timeouts, and a healthy pair settles zero timeouts"
}
