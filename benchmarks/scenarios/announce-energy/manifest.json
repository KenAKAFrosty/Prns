{
  "name": "announce-energy",
  "version": 1,
  "description": "Sustained announce ingest on all logical cores, measuring energy per announce (the price a battery/solar node actually pays). Shares the announce-parallel corpus; throughput here is the sustained average under continuous load, not the best-of-N peak.",
  "source": "shares ../announce-parallel/packets.hex (RNS 1.3.1 canonical; engine reproduces byte-for-byte)",
  "input": { "packets": "../announce-parallel/packets.hex", "encoding": "hex-per-line", "count": 2560 },
  "operations": { "ingest": "sustained-all-cores", "measure": "joules-per-announce" },
  "expected": { "route_count": 2560 }
}
