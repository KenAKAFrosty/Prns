{
  "name": "resource-64mib-stream",
  "version": 2,
  "title": "64 MiB resource stream",
  "category": "Resources",
  "summary": "Stream a 64 MiB incompressible resource with compression disabled.",
  "primary_metric": "goodput_bytes_per_sec",
  "headline": true,
  "notes": [],
  "description": "A single large resource transferred whole, over and over, for a fixed wall-time - the multi-segment bulk mechanism. Each logical transfer is 64 MiB sliced into MAX_EFFICIENT_SIZE segments, sent one at a time and proved before the next, so the engine and the host each hold a single segment while the receiver appends the stream to disk-sized totals. Against resource-max-segment (one segment) this measures whether the per-byte rate holds past the single-segment ceiling and whether peak memory stays flat at one segment regardless of total size. Compression is disabled on both stacks, keeping this a direct transport comparison.",
  "roles": [
    "responder",
    "initiator"
  ],
  "profile": {
    "mechanism": "resource",
    "payload_len": 67108864,
    "window": 1,
    "duration_ms": 30000,
    "compression": "off"
  },
  "conformance": "settled == received at the far end; every transfer is a sequence of hash-proved segments the receiver assembles by original_hash before it counts the whole"
}
