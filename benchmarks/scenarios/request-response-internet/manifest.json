{
  "name": "request-response-internet",
  "version": 1,
  "description": "Windowed RPC through a shaped pipe at an internet wire (50 Mbps, 25 ms one-way): remote-instance queries where the round trip dominates everything. Requests per second is window-bound physics; the protocol's job is to add nothing on top.",
  "roles": [
    "responder",
    "initiator"
  ],
  "profile": {
    "mechanism": "request",
    "request_min": 16,
    "request_max": 300,
    "response_min": 32,
    "response_max": 350,
    "window": 4,
    "duration_ms": 30000,
    "wire_shape": {
      "label": "internet",
      "rate_bps": 50000000,
      "latency_ms": 25
    }
  },
  "conformance": "sent == delivered + timeouts with zero timeouts expected - the pipe delays, it never drops"
}
